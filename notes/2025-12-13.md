GitOps is a way to make everything declarative. State can be found on Git, changes can be tracked. It also makes it so that an engineer doesnt have to touch kubectl, he can simply push changes to git and let a Kubemanager like Argo or Flux handle the deployment
